extends CanvasLayer


func _ready():
	# Apenas o caminho direto para o VideoStreamPlayer,
	# pois ele é um filho direto do nó principal da cena (que é o CanvasLayer).
	pass

# O restante do código pode ser removido, pois ele não é necessário
# para a lógica de reprodução do vídeo.
